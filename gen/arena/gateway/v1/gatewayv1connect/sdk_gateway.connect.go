// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: arena/gateway/v1/sdk_gateway.proto

package gatewayv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/moepig/arena/gen/arena/gateway/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SDKGatewayName is the fully-qualified name of the SDKGateway service.
	SDKGatewayName = "arena.gateway.v1.SDKGateway"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SDKGatewaySessionProcedure is the fully-qualified name of the SDKGateway's Session RPC.
	SDKGatewaySessionProcedure = "/arena.gateway.v1.SDKGateway/Session"
)

// SDKGatewayClient is a client for the arena.gateway.v1.SDKGateway service.
type SDKGatewayClient interface {
	// Session is the single multiplexed bidirectional stream a sidecar opens
	// at startup. On disconnect the sidecar reconnects with exponential
	// backoff; missed pushes are recovered by returning the current state
	// from DynamoDB on reconnect.
	Session(context.Context) *connect.BidiStreamForClient[v1.SidecarMessage, v1.GatewayMessage]
}

// NewSDKGatewayClient constructs a client for the arena.gateway.v1.SDKGateway service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSDKGatewayClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SDKGatewayClient {
	baseURL = strings.TrimRight(baseURL, "/")
	sDKGatewayMethods := v1.File_arena_gateway_v1_sdk_gateway_proto.Services().ByName("SDKGateway").Methods()
	return &sDKGatewayClient{
		session: connect.NewClient[v1.SidecarMessage, v1.GatewayMessage](
			httpClient,
			baseURL+SDKGatewaySessionProcedure,
			connect.WithSchema(sDKGatewayMethods.ByName("Session")),
			connect.WithClientOptions(opts...),
		),
	}
}

// sDKGatewayClient implements SDKGatewayClient.
type sDKGatewayClient struct {
	session *connect.Client[v1.SidecarMessage, v1.GatewayMessage]
}

// Session calls arena.gateway.v1.SDKGateway.Session.
func (c *sDKGatewayClient) Session(ctx context.Context) *connect.BidiStreamForClient[v1.SidecarMessage, v1.GatewayMessage] {
	return c.session.CallBidiStream(ctx)
}

// SDKGatewayHandler is an implementation of the arena.gateway.v1.SDKGateway service.
type SDKGatewayHandler interface {
	// Session is the single multiplexed bidirectional stream a sidecar opens
	// at startup. On disconnect the sidecar reconnects with exponential
	// backoff; missed pushes are recovered by returning the current state
	// from DynamoDB on reconnect.
	Session(context.Context, *connect.BidiStream[v1.SidecarMessage, v1.GatewayMessage]) error
}

// NewSDKGatewayHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSDKGatewayHandler(svc SDKGatewayHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	sDKGatewayMethods := v1.File_arena_gateway_v1_sdk_gateway_proto.Services().ByName("SDKGateway").Methods()
	sDKGatewaySessionHandler := connect.NewBidiStreamHandler(
		SDKGatewaySessionProcedure,
		svc.Session,
		connect.WithSchema(sDKGatewayMethods.ByName("Session")),
		connect.WithHandlerOptions(opts...),
	)
	return "/arena.gateway.v1.SDKGateway/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SDKGatewaySessionProcedure:
			sDKGatewaySessionHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSDKGatewayHandler returns CodeUnimplemented from all methods.
type UnimplementedSDKGatewayHandler struct{}

func (UnimplementedSDKGatewayHandler) Session(context.Context, *connect.BidiStream[v1.SidecarMessage, v1.GatewayMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("arena.gateway.v1.SDKGateway.Session is not implemented"))
}
